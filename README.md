# analyse-financiere-deputee
generate_data.js : Un script Node.js à exécuter une seule fois (ou via une tâche planifiée) pour télécharger le XML, le parser, calculer les participations cumulées et générer un fichier hatvp_data.json compact.
